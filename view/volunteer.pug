extends layout.pug
block login
    include menu/loginmenu.pug
block menu
    include menu/loggedmenu.pug
block content
    if message
        h1=message
    if infos
        each info in infos
            h1=info
    if emp.type=="Organization"   
        include viewevent.pug
    else
        include joinevent.pug